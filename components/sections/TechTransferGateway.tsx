"use client";

import { AnimatePresence, motion } from "framer-motion";
import { ArrowUpRight, Check, X } from "lucide-react";
import { useState } from "react";

interface Technology { id: string; name: string; category: string; description: string; fit: string; }
const technologies: Technology[] = [
  { id: "pslv", name: "PSLV Technology Transfer", category: "Launch systems", description: "Explore pathways for transferring proven launch vehicle technologies to Indian industry.", fit: "Launch, manufacturing, and aerospace companies" },
  { id: "feast", name: "FEAST Finite Element Software", category: "Simulation", description: "A structural analysis tool developed for space engineering and advanced design workflows.", fit: "Engineering software and simulation teams" },
  { id: "battery", name: "Lithium-Ion Cell Tech", category: "Energy systems", description: "Space-qualified cell technology for high-reliability energy storage applications.", fit: "Battery, mobility, and clean-tech innovators" }
];

export function TechTransferGateway() {
  const [active, setActive] = useState(technologies[0]);
  const [modal, setModal] = useState(false);
  return <section className="border-t border-white/10 bg-[#080C14] px-6 py-24 lg:px-10 lg:py-36"><div className="mx-auto max-w-[1440px]"><div className="grid gap-14 lg:grid-cols-[.85fr_1.15fr] lg:items-end"><div><p className="mb-5 text-[10px] uppercase tracking-[.32em] text-blue-400">From public research to public value</p><h2 className="text-5xl font-medium leading-[.9] tracking-tighter md:text-7xl">Open the next frontier.</h2><p className="mt-8 max-w-md text-sm leading-7 text-white/55">A gateway for Indian startups and industry to work with space-grade capabilities, facilities, and technology transfer pathways.</p><button onClick={() => setModal(true)} className="mt-10 inline-flex items-center gap-3 bg-ember px-5 py-4 text-[10px] font-bold uppercase tracking-[.2em] transition-colors hover:bg-[#ff7654]">Apply for ISRO Facility Testing <ArrowUpRight size={15} /></button></div><div className="border border-white/10 bg-black p-2"><div className="flex flex-wrap gap-1">{technologies.map((technology) => <button key={technology.id} onClick={() => setActive(technology)} className={`px-4 py-3 text-[10px] font-bold uppercase tracking-[.14em] transition-colors ${technology.id === active.id ? "bg-white text-black" : "text-white/45 hover:text-white"}`}>{technology.name}</button>)}</div><AnimatePresence mode="wait"><motion.div key={active.id} initial={{ opacity: 0, x: 12 }} animate={{ opacity: 1, x: 0 }} exit={{ opacity: 0, x: -12 }} className="min-h-[250px] border-t border-white/10 p-7 md:p-10"><p className="text-[10px] uppercase tracking-[.25em] text-ember">{active.category}</p><h3 className="mt-6 text-3xl font-medium tracking-tighter md:text-5xl">{active.name}</h3><p className="mt-5 max-w-lg text-sm leading-7 text-white/55">{active.description}</p><p className="mt-8 flex items-center gap-2 text-xs text-white/65"><Check size={15} className="text-emerald-400" />Best fit: {active.fit}</p></motion.div></AnimatePresence></div></div></div>{modal && <div className="fixed inset-0 z-[90] flex items-center justify-center bg-black/80 p-5 backdrop-blur-md"><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} className="relative w-full max-w-lg border border-white/15 bg-[#101622] p-7 md:p-10"><button onClick={() => setModal(false)} aria-label="Close startup registration" className="absolute right-5 top-5 text-white/50 hover:text-white"><X size={18} /></button><p className="text-[10px] uppercase tracking-[.25em] text-blue-400">IN-SPACe / Facility access</p><h3 className="mt-5 text-4xl font-medium tracking-tighter">Start your application.</h3><p className="mt-4 text-sm leading-6 text-white/55">Tell us about your company and the capability you want to test with ISRO.</p><div className="mt-8 space-y-4"><input placeholder="Company name" className="w-full border border-white/15 bg-black/30 p-4 text-sm text-white outline-none placeholder:text-white/25 focus:border-blue-400" /><input placeholder="Work email" type="email" className="w-full border border-white/15 bg-black/30 p-4 text-sm text-white outline-none placeholder:text-white/25 focus:border-blue-400" /><button onClick={() => setModal(false)} className="w-full bg-ember px-5 py-4 text-[10px] font-bold uppercase tracking-[.2em]">Register interest</button></div></motion.div></div>}</section>;
}
